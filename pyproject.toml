[tool.robotpy]
robotpy_version = "2026.2.1.1"

# Directories and files to exclude from deploy to the RoboRIO.
# This keeps sim-only, test, and docs files off the robot.
robotpy_excludes = [
    "tests",
    "docs",
    "scripts",
    "calibration",
    "ctre_sim",
    "generated",
    "testing",
    "hardware/mock_motor_controller.py",
    "constants/simulation.py",
]
